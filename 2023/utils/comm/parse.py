from typing import List
# lines: returns a list of lines (drop first and last lines if they are empty, for tests)
# sections: separated by \n\n
# grid: return a 2-D grid 
# dense matrix: [[1234], [5678], ...]

class Parser:
    data: str
    lines: List[str]
    sections: List[str]
    grid:

